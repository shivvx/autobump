const config = {
    TOKEN: 'YourToken',  //paste your dicord account token ( Not bot token xd.)
    BUMP_CHANNELS: ['12235623', '123456678'] //replace with channel id's and you can add multiple if want!
}

module.exports = config
